Start a clean LND instance (https://github.com/lightningnetwork/lnd)

Confirm paths in defaults.js (Windows needs modification and paths must use double backslashes)


npm install

node main -h 0.0.0.0  
